query CurriculaByCategorySlug($categorySlug: String!) {
  curricula(where: { slug: $categorySlug }) {
    id
    slug
    title
    description
    category {
      title
      slug
    }
    image {
      url
    }
  }
}
