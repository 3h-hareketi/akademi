query Curricula($stage: Stage! = PUBLISHED) {
  curricula(stage: $stage) {
    id
    slug
    title
    description
    category {
      title
      slug
    }
    image {
      url
    }
    articles {
      id
    }
  }
}
