query getArticlesByCurriculum($id: ID) {
  curriculum(where: { id: $id }) {
    title
    articles {
      id
      title
      order
      content {
        html
      }
      choices {
        choice
      }
    }
  }
}
