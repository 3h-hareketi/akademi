query CurriculumBySlug($slug: String!) {
  curriculum(where: { slug: $slug }) {
    title
    slug
    description
    image {
      fileName
      url
    }
    category {
      title
    }
    articles {
      id
      title
    }
  }
}
