overwrite: true
schema: "${GRAHPCMS_ENDPOINT}"
documents: "graphql/**/*.graphql"
generates:
  interfaces/index.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
