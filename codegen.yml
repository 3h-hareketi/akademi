overwrite: true
schema: "${GRAPHCMS_ENDPOINT}"
documents: "graphql/**/*.graphql"
generates:
  interfaces/index.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
